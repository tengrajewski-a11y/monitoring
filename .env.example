# Lokalna baza SQLite (w wersji desktopowej Electron ustawia to automatycznie
# main.js, wskazując na katalog danych użytkownika)
DATABASE_URL="file:./dev.db"

# Opcjonalny sekret zabezpieczający POST /api/ingest (np. do wywołania z crona)
# INGEST_SECRET="zmien-mnie"
